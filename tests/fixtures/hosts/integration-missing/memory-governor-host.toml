version = "0.1"
profile = "generic"

[integration]
host_entry_paths = ["HOST.md"]
writer_contract_paths = ["skills/example-writer/SKILL.md"]

[targets.system_rules]
mode = "single"
paths = ["HOST.md"]
structured = false
